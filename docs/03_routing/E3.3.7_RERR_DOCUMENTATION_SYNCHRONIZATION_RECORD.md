# Documentation Synchronization Record — RERR Contract Freeze

**Date:** 2026-08-25  
**Baseline:** ENP-0.2-r5(20260825-203116)

## Synchronization decision

The project status now distinguishes three things:

1. historical audit documents;
2. authoritative current protocol/implementation contract;
3. the next RERR implementation workstream.

Historical documents are preserved. They are not silently rewritten to erase
their original state.

## Corrected contradiction

The draft RERR/protocol documentation contained an alternative reason-code
mapping that is not authoritative.

That older protocol contract is explicitly classified as:

**HISTORICAL / SUPERSEDED**

It is retained only to preserve documentation history. It must not be used by
production code, tests, or new protocol documentation. The frozen
`enp_routing.h/.c` mapping is the sole authoritative ENP v0.2 reason-code
contract.

The authoritative contract is:

```text
1 = NO_ROUTE
2 = NEXT_HOP_UNREACHABLE
3 = ROUTE_EXPIRED
4 = LOCAL_REPAIR_FAILED
5 = TTL_EXPIRED
```

## Current gate state

```text
P4-E5B          FROZEN
P4-E5C          FROZEN
P4-E5D Step-3   FROZEN
P4-E5E-I33      HARDWARE VALIDATED / FROZEN

RERR reason contract     FROZEN
RERR implementation      NOT YET OPEN
```

## Required synchronization rule

Any future RERR document, test or implementation must use the canonical
reason-code symbols from `enp_routing.h` and must not introduce alternate
numeric meanings without an explicit protocol architecture review.

## No production changes

This synchronization changes documentation only.
