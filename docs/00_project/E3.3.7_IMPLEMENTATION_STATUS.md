# E3.3.7 Implementation Status

**Target:** ENP v0.2-r5 / ESP-IDF 6.0.2

**Status:** Phase 3 / E3C three-node reliability hardware validated; E3.3.7 integration sequence frozen.

## Completed

### Phase 1 — Reliability Core

- Fixed-size reliability transaction table (`8` entries).
- `ACK_REQUIRED` uses the existing common ENP packet flag.
- Reliability retains a static copy of the complete `enp_packet_t` for retransmission.
- ACK correlation validates source, destination, DATA packet sequence and application sequence.
- ACK timeout processing and retry accounting.
- Duplicate ACK completion suppression.
- Delivery success/failure result callbacks.
- Transport/routing-independent submit callback.
- Phase 1 self-test and real ESP validation completed.

### Phase 2 — Dispatcher Integration

- Dispatcher initialization.
- Reliability ACK service registration.
- ACK correlation through the dispatcher service path.
- Duplicate ACK handling.
- Timeout and retransmission validation.
- Transaction identity preservation.
- Real ESP validation completed.

### Phase 3 / E1 — Routing Data Path

- Active route lookup.
- Next-hop selection.
- Logical next-hop to transport-address resolution callback.
- Origin DATA submission without modifying the packet.
- Forwarding with TTL decrement and CRC reseal.
- Real ESP hardware validation completed.
- E1 frozen.

### Phase 3 / E2-A — Context + Neighbor Integration

- Real ENP context.
- Real neighbor table.
- Logical-to-transport address resolution.
- Routing data path integration with the real context.
- Static storage correction after initial main-task stack overflow.
- Corrected implementation passed on real ESP hardware.
- E2-A frozen.

### Phase 3 / E2-B — Real ESP-NOW Integration

- Real ESP-NOW transport.
- Two-node origin DATA transmission.
- Actual remote ESP-NOW reception.
- ENP frame validation at the receiving node.
- Source, destination, sequence, flags, TTL and payload-length validation.
- Two-node hardware validation completed.
- E2-B frozen.

## Current Phase — Phase 3 / E3

The API audit found that the existing reliability submit callback is already the
smallest suitable integration boundary:

```text
Reliability
    |
    | enp_reliability_submit_fn
    v
Routing DATA path
    |
    +-- current route lookup
    +-- current next-hop selection
    +-- logical -> transport resolution
    +-- transport send
```

No new reliability API is required. Reliability owns the transaction and packet
identity; routing determines the current path for each transmission attempt.

The E3 self-test also deliberately changes the active route between the initial
submission and a retransmission. This proves that a retry re-enters routing and
can select a new next hop without changing the reliability transaction identity.

## Route-failure/resilience boundary

E3 does not implement route failure detection, RERR, route rediscovery or route
repair. Those behaviours must be implemented later in routing/resilience. The
E3 integration is deliberately designed so that reliability does not cache or
own a routing next-hop decision.

Future example:

```text
Attempt 1: A -> B -> C

B/C path becomes unavailable and routing repairs the route

Attempt 2: A -> E -> C

Same reliability transaction; different current route.
```

## Current validation matrix

```text
E3.3.7 Phase 1 / Reliability Core                 PASS
E3.3.7 Phase 2 / Dispatcher Integration           PASS
Phase 3 / E1 Routing Data Path                    PASS
Phase 3 / E2-A Context + Neighbor                 PASS
Phase 3 / E2-B Real ESP-NOW                       PASS
Phase 3 / E3 Reliability -> Routing           PASS / FROZEN
Phase 3 / E3B two-node ESP-NOW integration    PASS / FROZEN
Phase 3 / E3C three-node reliability          PASS / FROZEN
Final E3.3.7 integration sequence             PASS / FROZEN
Routing resilience / RERR / repair               FUTURE
```

## Next steps

1. Consolidate the validated E3 components into reusable ENP infrastructure and reduce the E3 test applications to thin integration tests.
2. Keep routing resilience (stale/lost next hops, RERR, route rediscovery/repair) as a separate subsequent phase.
3. Preserve the frozen E3A/E3B/E3C contracts while integrating the proven behavior into production services.


### Phase 3 / E3A current status

The reliability-to-routing self-test was implemented and validated on ESP32 hardware. The first run exposed an incorrect test assumption about transport receive-callback registration; the test was corrected to use the existing `enp_transport_set_receive_callback()` API explicitly. The corrected self-test passed all checks. E3A is frozen.


### Phase 3 / E3B hardware status

E3B passed on two real ESP32 nodes. The test validated real ESP-NOW DATA
transmission, deliberate first-ACK loss, reliability retransmission, ACK
return, dispatcher processing and final `DELIVERED` state. E3B is frozen.

### Phase 3 / E3C status

E3C passed on the three-node A -> B -> C topology. It validated A -> B -> C DATA, C -> B -> A ACK, deliberate first-ACK loss, reliability retransmission, duplicate DATA suppression at B, cached ACK recovery, exactly-once application delivery at C, and final `DELIVERED` completion at A. E3C is frozen. Route failure/repair remains outside E3.3.7.
