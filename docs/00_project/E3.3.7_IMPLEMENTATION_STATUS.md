# E3.3.7 Implementation Status

**Target:** ENP v0.2-r5 / ESP-IDF 6.0.2

**Status:** Phase 4 consolidation validated through P4/E1, P4/E2, P4/E3, P4-E4A, P4-E4B, P4-E4C, P4-E4D, P4-E5A, P4-E5B and hardware-validated P4-E5C. P4-E4C/P4-E4D are HARDWARE VALIDATED / FROZEN; P4-E5A is VALIDATED / FROZEN by controlled self-test; P4-E5B is HARDWARE VALIDATED / FROZEN.

## Completed

### Phase 1 — Reliability Core

- Fixed-size reliability transaction table (`8` entries).
- `ACK_REQUIRED` uses the existing common ENP packet flag.
- Reliability retains a static copy of the complete `enp_packet_t` for retransmission.
- ACK correlation validates source, destination, DATA packet sequence and application sequence.
- ACK timeout processing and retry accounting.
- Duplicate ACK completion suppression.
- Delivery success/failure result callbacks.
- Transport/routing-independent submit callback.
- Phase 1 self-test and real ESP validation completed.

### Phase 2 — Dispatcher Integration

- Dispatcher initialization.
- Reliability ACK service registration.
- ACK correlation through the dispatcher service path.
- Duplicate ACK handling.
- Timeout and retransmission validation.
- Transaction identity preservation.
- Real ESP validation completed.

### Phase 3 / E1 — Routing Data Path

- Active route lookup.
- Next-hop selection.
- Logical next-hop to transport-address resolution callback.
- Origin DATA submission without modifying the packet.
- Forwarding with TTL decrement and CRC reseal.
- Real ESP hardware validation completed.
- E1 frozen.

### Phase 3 / E2-A — Context + Neighbor Integration

- Real ENP context.
- Real neighbor table.
- Logical-to-transport address resolution.
- Routing data path integration with the real context.
- Static storage correction after initial main-task stack overflow.
- Corrected implementation passed on real ESP hardware.
- E2-A frozen.

### Phase 3 / E2-B — Real ESP-NOW Integration

- Real ESP-NOW transport.
- Two-node origin DATA transmission.
- Actual remote ESP-NOW reception.
- ENP frame validation at the receiving node.
- Source, destination, sequence, flags, TTL and payload-length validation.
- Two-node hardware validation completed.
- E2-B frozen.

## Current Phase — Phase 4 / P4

The API audit found that the existing reliability submit callback is already the
smallest suitable integration boundary:

```text
Reliability
    |
    | enp_reliability_submit_fn
    v
Routing DATA path
    |
    +-- current route lookup
    +-- current next-hop selection
    +-- logical -> transport resolution
    +-- transport send
```

No new reliability API is required. Reliability owns the transaction and packet
identity; routing determines the current path for each transmission attempt.

The E3 self-test also deliberately changes the active route between the initial
submission and a retransmission. This proves that a retry re-enters routing and
can select a new next hop without changing the reliability transaction identity.

## Route-failure/resilience boundary

E3 does not implement route failure detection, RERR, route rediscovery or route
repair. Those behaviours must be implemented later in routing/resilience. The
E3 integration is deliberately designed so that reliability does not cache or
own a routing next-hop decision.

Future example:

```text
Attempt 1: A -> B -> C

B/C path becomes unavailable and routing repairs the route

Attempt 2: A -> E -> C

Same reliability transaction; different current route.
```

## Current validation matrix

```text
Phase 1 / Reliability Core                                         PASS
Phase 2 / Dispatcher Integration                                   PASS
Phase 3 / E1 Routing Data Path                                     PASS
Phase 3 / E2-A Context + Neighbor                                  PASS
Phase 3 / E2-B Real ESP-NOW                                        PASS
Phase 3 / E3 Reliability -> Routing                             PASS / FROZEN
Phase 3 / E3B two-node ESP-NOW integration                      PASS / FROZEN
Phase 3 / E3C three-node reliability                            PASS / FROZEN
Phase 4 / P4-E1 Reusable Data Plane                             PASS / FROZEN
Phase 4 / P4-E2 Reliability Maintenance                         PASS / FROZEN
Phase 4 / P4-E3 E3C consolidation                               PASS / FROZEN
Phase 4 / P4-E4A Dispatcher Local Dispatch                      PASS / FROZEN
Phase 4 / P4-E4B Production Receive Path                        PASS / FROZEN
Phase 4 / P4-E5A Next-Hop Failure Observation                   PASS / FROZEN
Phase 4 / P4-E5B Real ESP-NOW TX-Result Observation      HARDWARE VALIDATED / FROZEN
Phase 4 / P4-E5C Transport Failure → Route Invalidation  HARDWARE VALIDATED / FROZEN
Routing resilience / RERR / repair                                 FUTURE
```

## Next steps

1. Consolidate the validated E3 components into reusable ENP infrastructure and reduce the E3 test applications to thin integration tests.
2. Keep routing resilience (stale/lost next hops, RERR, route rediscovery/repair) as a separate subsequent phase.
3. Preserve the frozen E3A/E3B/E3C contracts while integrating the proven behavior into production services.


### Phase 3 / E3A current status

The reliability-to-routing self-test was implemented and validated on ESP32 hardware. The first run exposed an incorrect test assumption about transport receive-callback registration; the test was corrected to use the existing `enp_transport_set_receive_callback()` API explicitly. The corrected self-test passed all checks. E3A is frozen.


### Phase 3 / E3B hardware status

E3B passed on two real ESP32 nodes. The test validated real ESP-NOW DATA
transmission, deliberate first-ACK loss, reliability retransmission, ACK
return, dispatcher processing and final `DELIVERED` state. E3B is frozen.

### Phase 3 / E3C status

E3C passed on the three-node A -> B -> C topology. It validated A -> B -> C DATA, C -> B -> A ACK, deliberate first-ACK loss, reliability retransmission, duplicate DATA suppression at B, cached ACK recovery, exactly-once application delivery at C, and final `DELIVERED` completion at A. E3C is frozen. Route failure/repair remains outside E3.3.7.


## Phase 4 / E1 — Reusable Data Plane

**Status:** PASS / FROZEN.

P4.1 introduces `core/data/enp_data_plane.*` and moves reusable receive-time
DATA/ACK duplicate-domain ownership and non-local DATA/ACK forwarding into
ENP core infrastructure. The existing routing data path remains the owner of
route lookup, next-hop resolution, TTL decrement and transport transmission.

The existing dispatcher API remains compatible. `enp_dispatcher_dispatch_local()`
was added for the future data-plane receive path so that DATA/ACK packets are not duplicate-filtered twice.


## Phase 4 / E2 — Reliability Maintenance

**Status:** PASS / FROZEN.

The existing static ENP maintenance task now invokes `enp_reliability_tick(now_ms)`.
The reliability layer remains transport-independent and FreeRTOS-independent.
The hardware-independent self-test verifies one maintenance-driven retry,
preserved transaction identity, correlated ACK completion, and final
`DELIVERED` state. The test uses a total submission count of two
(initial submission plus one retransmission) and a reliability retry count of one.


### Phase 4 / P4-E4A — Dispatcher Local Dispatch

**Status:** PASS / FROZEN.

The local-dispatch boundary is validated. DATA/ACK local delivery bypasses the generic dispatcher duplicate cache, while normal dispatcher dispatch retains generic duplicate suppression.

### Phase 4 / P4-E4B — Production Receive-Path Integration

**Status:** PASS / FROZEN.

The production receive path is validated: DATA/ACK traffic enters the reusable data plane; other packet types remain on the normal dispatcher path. Non-local DATA reaches the existing routing data path with TTL decrement, identity preservation and configured next-hop selection.

### Phase 4 / E3 — E3C Consolidation

**Status:** PASS / FROZEN.

P4/E3 consolidates the reusable DATA/ACK receive-time behavior proven by the
Phase 3 E3C three-node test into `core/data/enp_data_plane.*`. The E3C
integration test no longer owns the production DATA/ACK duplicate caches,
DATA forwarding, ACK forwarding, or cached-ACK recovery logic.

The validated hardware path is:

```text
A -> B -> C DATA
C -> B -> A ACK

First ACK is intentionally dropped at A before A data-plane processing.
A retransmits the same DATA transaction.
B suppresses the duplicate DATA and recovers the cached ACK.
B forwards the recovered ACK to A.
A completes the same reliability transaction as DELIVERED.
```

The three-node hardware validation was performed with the controlled startup order `C -> B -> A`, so that C and B are ready before A submits the initial reliable DATA. This startup order is a property of the current hardware test harness and is not an ENP protocol requirement.

B validation counters were observed as:

```text
DATA received              2
DATA forwarded             1
ACK received               1
ACK forwarded              2
duplicate DATA suppressed  1
cached ACK recovered       1
```

C delivered application DATA exactly once and generated exactly one ACK. A observed exactly one retransmission, received the recovered ACK, and reached `DELIVERED`.
Non-DATA/ACK traffic, including periodic Discovery packets, is excluded from the reusable data plane; the data plane remains responsible for DATA and ACK traffic rather than discovery/routing-control packet processing.


### Phase 4 / P4-E4A — Dispatcher Local Dispatch

**Status:** PASS / FROZEN.

The local-dispatch boundary is validated. DATA/ACK local delivery bypasses the generic dispatcher duplicate cache, while normal dispatcher dispatch retains generic duplicate suppression.

### Phase 4 / P4-E4B — Production Receive-Path Integration

**Status:** PASS / FROZEN.

The production receive path is validated: DATA/ACK traffic enters the reusable data plane; other packet types remain on the normal dispatcher path. Non-local DATA reaches the existing routing data path with TTL decrement, identity preservation and configured next-hop selection.

## P4-E4D — Production Runtime Hardware Validation

**Status:** HARDWARE VALIDATED / FROZEN (2026-08-17)

See `docs/05_validation/E3.3.7_PHASE4_E4D_PRODUCTION_RUNTIME_HARDWARE_VALIDATION_IDF6.0.2.md`.


## P4-E5A — Next-Hop Failure Detection and Propagation

**Status:** VALIDATED / FROZEN (2026-08-17)

P4-E5A implements the approved transport send-result observation boundary.
The controlled self-test demonstrated successful and failed transmission
results being propagated through the ENP transport callback while preserving
transport destination identity.

P4-E5A deliberately does not perform route invalidation, RERR, route discovery,
route repair, or reliability recovery.

**Hardware status:** NOT YET HARDWARE VALIDATED.

Validation record: `docs/05_validation/E3.3.7_PHASE4_P4-E5A_VALIDATION_RECORD.md`


## P4-E5C — Transport Failure → Route Invalidation

**Status:** VALIDATED / FROZEN (controlled self-test, 2026-08-18).

P4-E5C consumes the frozen P4-E5B send-result interface and invalidates every
ACTIVE route whose logical next hop resolves to the failed transport
destination. The controlled self-test passed all defined acceptance criteria,
including shared-next-hop invalidation, unrelated-route preservation,
sequence preservation, idempotency, and successful-result immunity.

Validation record:
`docs/05_validation/E3.3.7_PHASE4_P4-E5C_IMPLEMENTATION.md`

**Hardware validation remains pending.**


---

## P4-E5D — Route Repair / R4 Rediscovery — Step 3

**Status: HARDWARE VALIDATED / FROZEN — 2026-08-23**

Step-3 connects the validated E5D route-repair coordinator to the existing R4
discovery implementation through a thin orchestration adapter.

Frozen ownership:

- E5D owns repair coordination;
- R4-A owns discovery state/lifecycle;
- R4-B owns RREQ processing;
- R4-C owns RREP processing;
- the route table owns route state and metadata;
- transport remains below routing.

The Step-3 controlled validation passed the expanded lifecycle suite, including
failed-next-hop exclusion, stale sequence preservation, duplicate suppression,
queued repairs, timeout/retry exhaustion, old-RREP rejection and unrelated-route
isolation.

The V3.4 three-node hardware validation then demonstrated:

```text
real DATA -> failed B
    -> E5C failure
    -> E5D repair
    -> real RREQ A -> C
    -> real RREP C -> A
    -> R4 completion
    -> route C ACTIVE via C
    -> sequence 7 preserved
```

No RREQ/RREP wire-format change or R4-A/B/C ownership change was introduced.

Validation records:

- `docs/05_validation/E3.3.7_PHASE4_P4-E5D_STEP3_CONTROLLED_VALIDATION_EXPANSION.md`
- `docs/05_validation/E3.3.7_PHASE4_P4-E5D_STEP3_HARDWARE_VALIDATION_PLAN.md`
- `docs/05_validation/E3.3.7_PHASE4_P4-E5D_STEP3_HARDWARE_VALIDATION_RECORD.md`
- `docs/05_validation/E3.3.7_PHASE4_P4-E5D_STEP3_FINAL_IMPLEMENTATION_AUDIT.md`

**Freeze rule:** no further Step-3 functionality is to be added under this
baseline. Subsequent changes require a new Step-3 revision/phase and a new
audit/regression/validation cycle.
